# DB Schema
SQLite normalized schema is implemented in `src/database/migrations/001_initial.sql` and mirrored in runtime migration `001_initial.ts`. Includes all mandatory tables, foreign keys, and FTS virtual tables.
